/**
 * Declaring a variable
 * 
 * 1) var - not using anymore
 * 2) let 
 * 3) const
 * 
 * after this tutorial, let and const using more
 * but var is used to let you know 
 * "where the functions coming from?"
 * 
 * but, we need to know var-keyword to understand
 * codes from various projects.
 */

var name = 'code_factory';
console.log(name);

var age = 32;
console.log(age);

// how to use let and const
let ive = 'Ive';
console.log(ive);

/**
 * let and var can change the values of variables
 * after declaration. 
 */

ive = 'Ann';
console.log(ive)

const newJeans = 'Hyerion'; // not chaging in future
console.log(newJeans);

// newJeans = 'Code_Factory'; // type error is coming

/**
 * 1) identifying and declaring variables first. 
 * 2) value input into the declared variables. 
 * 
 */
var name; // going to use in future. 
name = 'code_factory';
console.log(name);

let girl;
console.log(girl); // return is 'undefined' at the console. 

// const girl2; const cannot be left with the status 'Undefined'
// because const cannot be changed in future. with the same process
// the undefined const also not changing. 
// so the undefined const is 'error'




